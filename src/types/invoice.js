// Este archivo ahora solo sirve como referencia para la estructura del objeto
// ya que JavaScript no tiene tipos estáticos como TypeScript

/*
Estructura de ejemplo:
{
  invoiceNumber: string;
  date: string;
  dueDate: string;
  customerDetails: {
    name: string;
    address: string;
    email: string;
    phone: string;
  };
  items: Array<{
    description: string;
    quantity: number;
    unitPrice: number;
    total: number;
  }>;
  subtotal: number;
  tax: number;
  total: number;
  notes?: string;
}
*/
